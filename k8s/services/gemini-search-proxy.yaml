apiVersion: v1
kind: Service
metadata:
  name: gemini-search-proxy
  namespace: librechat
spec:
  selector:
    app: gemini-search-proxy
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
