apiVersion: v1
kind: Service
metadata:
  name: openai-search-proxy
  namespace: librechat
spec:
  selector:
    app: openai-search-proxy
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
